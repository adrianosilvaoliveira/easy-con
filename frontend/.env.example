# Desenvolvimento (API no mesmo host que o Vite proxy ou servidor unificado)
VITE_API_URL=http://localhost:3333/api

# Produção na Vercel: a plataforma define VITE_BACKEND_URL=/_/backend (não precisa configurar)
# Override manual, se necessário:
# VITE_API_URL=/_/backend/api