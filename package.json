{
  "name": "easy-stock",
  "version": "1.0.0",
  "private": true,
  "description": "Easy Stock — um servidor (API + frontend)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build:server --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
